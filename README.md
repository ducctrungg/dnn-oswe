# Setup lab for Dotnetnuke

1. Run `docker-compose up --build -d`
2. Access `http://localhost:8000/Install/InstallWizard.aspx`
3. For DB, setup with following information:

   - Server: `db\SQLEXPRESS`
   - User: `DNNSQLUser`
   - Password: `*OSWE#1234`
   - Database: `DNNDatabase`
